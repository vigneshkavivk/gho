version: "3.9"

services:
  python-app:
    build: .
    container_name: python-app
    expose:
      - 5000

  nginx:
    build: ./nginx
    container_name: nginx
    ports:
      - "8087:80"
    depends_on:
      - python-app
      
