apiVersion: v1
kind: Service
metadata:
  name: ghost
  namespace: ghost-blog
spec:
  selector:
    app: ghost
  ports:
    - name: http  # <-- FIXED: Changed from ghost-service to http
      protocol: TCP
      port: 2368
      targetPort: 2368
  type: NodePort
