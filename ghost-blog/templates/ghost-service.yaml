apiVersion: v1
kind: Service
metadata:
  name: ghost
  namespace: ghost-blog
spec:
  selector:
    app: ghost
  ports:
    - name: http
      protocol: TCP
      port: 2368
      targetPort: 2368
  type: ClusterIP
