apiVersion: v1
kind: Service
metadata:
  name: ghost
  namespace: ghost-blog
spec:
  selector:
    app: ghost
  ports:
    - name: http
      protocol: TCP
      port: 80         # Service port exposed to ALB
      targetPort: 2368 # Ghost listens on 2368 internally
  type: ClusterIP
